{
  "name": "ssh-mcp",
  "version": "1.0.0",
  "description": "MCP server for executing commands and managing files on remote hosts via SSH",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "ssh-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "ssh2": "^1.16.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/ssh2": "^1.15.5",
    "typescript": "^5.7.0"
  }
}
